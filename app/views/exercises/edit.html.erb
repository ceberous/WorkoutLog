<div class="row">
<div class="col-lg-6 col-lg-offset-3 col-md-6 col-md-offset-3 col-sm-6 col-sm-offset-3 col-xs-12">

<h1 class="tcenter">Editing <%= @exercise.name %></h1>

<%= simple_form_for([@workout , @exercise]) do |f| %>

	<% if @workout.errors.any? %>
    	<div id="error_explanation">
      		<h2><%= pluralize(@exercise.errors.count, "error") %> prohibited this todo_list from being saved:</h2>

      		<ul>
      			<% @exercise.errors.full_messages.each do |message| %>
        			<li><%= message %></li>
      			<% end %>
      		</ul>
    	</div>
  	<% end %>

  	<%= f.input :name , placeholder: "Name" , class: "form-control" %>
  	<%= f.input :sets , placeholder: "Number of Sets" , class: "form-control" %>
  	<%= f.input :reps , placeholder: "Repetitions" , class: "form-control" %>

  	<%= f.submit class: "btn btn-success form-control" %>
		
<% end %>

</div>
</div>